# Answer from versioned source snapshots

Every answer route uses a versioned captured source collection and the graph derived from that evidence. Query-time investigation is limited to bounded text and graph tools within that snapshot; obtaining newer sources belongs to the separate ingestion and refresh workflow. This trades immediate live-web freshness for reproducible evidence, comparable evaluations, and controllable query costs.
